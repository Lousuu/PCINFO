<UserControl x:Class="HardwareVision.Views.AdvancedSensorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:HardwareVision.ViewModels"
             Background="{StaticResource AppBackgroundBrush}"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:AdvancedSensorsViewModel, IsDesignTimeCreatable=True}">
    <Grid Style="{StaticResource RetroPageGridStyle}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource SensorRow}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel>
                    <TextBlock Text="传感器列表" Style="{StaticResource SectionTitleText}" />
                </StackPanel>
                <Border Grid.Column="1"
                        HorizontalAlignment="Right"
                        Style="{StaticResource StatusBadge}">
                    <TextBlock Text="{Binding SensorRows.Count, StringFormat={}{0} ROWS}"
                               Style="{StaticResource BadgeText}" />
                </Border>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource MetricCard}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.35*" MinWidth="150" />
                        <ColumnDefinition Width="1.65*" MinWidth="180" />
                        <ColumnDefinition Width="0.75*" MinWidth="86" />
                        <ColumnDefinition Width="0.35*" MinWidth="46" />
                        <ColumnDefinition Width="0.65*" MinWidth="72" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="名称" Style="{StaticResource MetricLabelText}" />
                    <TextBlock Grid.Column="1" Text="技术名" Style="{StaticResource MetricLabelText}" />
                    <TextBlock Grid.Column="2" HorizontalAlignment="Right" Text="数值" Style="{StaticResource MetricLabelText}" />
                    <TextBlock Grid.Column="3" HorizontalAlignment="Center" Text="单位" Style="{StaticResource MetricLabelText}" />
                    <TextBlock Grid.Column="4" Text="可用性" Style="{StaticResource MetricLabelText}" />
                </Grid>

                <ListBox Grid.Row="1"
                         Padding="0"
                         Background="Transparent"
                         BorderThickness="0"
                         ItemsSource="{Binding SensorRows}"
                         ScrollViewer.CanContentScroll="True"
                         ScrollViewer.IsDeferredScrollingEnabled="True"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="ListBoxItem">
                                        <ContentPresenter />
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <VirtualizingStackPanel />
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemTemplate>
                            <DataTemplate DataType="{x:Type viewModels:DetailSensorRowViewModel}">
                                <Border Margin="0,0,0,4"
                                        ToolTip="{Binding FullToolTip}"
                                        Style="{StaticResource SensorRow}">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="1.35*" MinWidth="150" />
                                            <ColumnDefinition Width="1.65*" MinWidth="180" />
                                            <ColumnDefinition Width="0.75*" MinWidth="86" />
                                            <ColumnDefinition Width="0.35*" MinWidth="46" />
                                            <ColumnDefinition Width="0.65*" MinWidth="72" />
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FullName}" Style="{StaticResource BodyText}" />
                                        <TextBlock Grid.Column="1" Text="{Binding DisplayType}" TextTrimming="CharacterEllipsis" ToolTip="{Binding FullType}" Style="{StaticResource BodyText}" />
                                        <TextBlock Grid.Column="2" HorizontalAlignment="Right" Text="{Binding Value}" TextTrimming="CharacterEllipsis" Style="{StaticResource BodyText}" />
                                        <TextBlock Grid.Column="3" HorizontalAlignment="Center" Text="{Binding Unit}" TextTrimming="CharacterEllipsis" Style="{StaticResource BodyText}" />
                                        <TextBlock Grid.Column="4" Text="{Binding Availability}" TextTrimming="CharacterEllipsis" Style="{StaticResource BodyText}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
