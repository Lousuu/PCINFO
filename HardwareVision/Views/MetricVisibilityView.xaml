<UserControl x:Class="HardwareVision.Views.MetricVisibilityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:layouts="clr-namespace:HardwareVision.Views.MetricVisibility"
             xmlns:models="clr-namespace:HardwareVision.Models"
             xmlns:themes="clr-namespace:HardwareVision.Themes"
             Background="{DynamicResource AppBackgroundBrush}">
    <UserControl.Resources>
        <DataTemplate x:Key="ClassicMetricVisibilityTemplate">
            <layouts:ClassicMetricVisibilityLayout />
        </DataTemplate>
        <DataTemplate x:Key="TraceworkMetricVisibilityTemplate">
            <layouts:TraceworkMetricVisibilityLayout />
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="MetricVisibilityLayoutHost"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Content="{Binding}">
        <ContentControl.Style>
            <Style TargetType="ContentControl">
                <Setter Property="ContentTemplate" Value="{StaticResource ClassicMetricVisibilityTemplate}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=(themes:ThemeContext.CurrentTheme), RelativeSource={RelativeSource Self}}"
                                 Value="{x:Static models:AppTheme.Tracework}">
                        <Setter Property="ContentTemplate" Value="{StaticResource TraceworkMetricVisibilityTemplate}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ContentControl.Style>
    </ContentControl>
</UserControl>
