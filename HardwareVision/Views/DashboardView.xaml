<UserControl x:Class="HardwareVision.Views.DashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewModels="clr-namespace:HardwareVision.ViewModels"
             Background="{StaticResource AppBackgroundBrush}"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:DashboardViewModel, IsDesignTimeCreatable=True}">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled"
                  VerticalScrollBarVisibility="Auto">
        <StackPanel Style="{StaticResource RetroPageStackStyle}">
            <Border Style="{StaticResource SensorRow}">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="1.1*" />
                        <ColumnDefinition Width="1.2*" />
                        <ColumnDefinition Width="1.6*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>

                    <StackPanel>
                        <TextBlock Text="应用" Style="{StaticResource MetricLabelText}" />
                        <TextBlock Text="{Binding ApplicationName}" Style="{StaticResource SectionTitleText}" />
                    </StackPanel>

                    <StackPanel Grid.Column="1">
                        <TextBlock Text="当前设备" Style="{StaticResource MetricLabelText}" />
                        <TextBlock Text="{Binding DeviceName}"
                                   TextTrimming="CharacterEllipsis"
                                   Style="{StaticResource BodyText}" />
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="系统版本" Style="{StaticResource MetricLabelText}" />
                        <TextBlock Text="{Binding OperatingSystem}"
                                   TextTrimming="CharacterEllipsis"
                                   Style="{StaticResource BodyText}" />
                    </StackPanel>

                    <StackPanel Grid.Column="3" HorizontalAlignment="Right">
                        <TextBlock Text="最后刷新" Style="{StaticResource MetricLabelText}" />
                        <TextBlock Text="{Binding LastRefreshTime}" Style="{StaticResource BodyText}" />
                    </StackPanel>
                </Grid>
            </Border>

            <ItemsControl Margin="0,10,0,0" ItemsSource="{Binding OverviewCards}">
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Visibility"
                                Value="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <UniformGrid Columns="3" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type viewModels:HardwareOverviewCardViewModel}">
                        <Border Margin="0,0,8,8"
                                ToolTip="{Binding ToolTip}"
                                Style="{StaticResource MetricCard}">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <TextBlock Text="{Binding Title}" Style="{StaticResource MetricLabelText}" />

                                <TextBlock Grid.Row="1"
                                           Margin="0,4,0,8"
                                           Text="{Binding HardwareName}"
                                           TextTrimming="CharacterEllipsis"
                                           Style="{StaticResource BodyText}" />

                                <ItemsControl Grid.Row="2" ItemsSource="{Binding Metrics}">
                                    <ItemsControl.ItemContainerStyle>
                                        <Style TargetType="ContentPresenter">
                                            <Setter Property="Visibility"
                                                    Value="{Binding IsVisible, Converter={StaticResource BoolToVisibilityConverter}}" />
                                        </Style>
                                    </ItemsControl.ItemContainerStyle>
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate DataType="{x:Type viewModels:DetailMetricViewModel}">
                                            <Grid Margin="0,0,0,4"
                                                  ToolTip="{Binding ToolTip}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>

                                                <TextBlock Text="{Binding Label}"
                                                           TextTrimming="CharacterEllipsis"
                                                           Style="{StaticResource MetricLabelText}" />
                                                <TextBlock Grid.Column="1"
                                                           Margin="10,0,0,0"
                                                           Text="{Binding Value}"
                                                           TextTrimming="CharacterEllipsis"
                                                           Style="{StaticResource BodyText}" />
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>
    </ScrollViewer>
</UserControl>
