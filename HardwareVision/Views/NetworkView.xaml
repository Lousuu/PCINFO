<UserControl x:Class="HardwareVision.Views.NetworkView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="clr-namespace:HardwareVision.Models"
             xmlns:network="clr-namespace:HardwareVision.Views.Network"
             xmlns:themes="clr-namespace:HardwareVision.Themes"
             xmlns:viewModels="clr-namespace:HardwareVision.ViewModels"
             Background="{DynamicResource AppBackgroundBrush}"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewModels:NetworkViewModel, IsDesignTimeCreatable=True}">
    <UserControl.Resources>
        <DataTemplate x:Key="ClassicNetworkTemplate">
            <network:ClassicNetworkLayout />
        </DataTemplate>
        <DataTemplate x:Key="TraceworkNetworkTemplate">
            <network:TraceworkNetworkLayout />
        </DataTemplate>
    </UserControl.Resources>

    <ContentControl x:Name="NetworkLayoutHost"
                    HorizontalContentAlignment="Stretch"
                    VerticalContentAlignment="Stretch"
                    Content="{Binding}">
        <ContentControl.Style>
            <Style TargetType="ContentControl">
                <Setter Property="ContentTemplate" Value="{StaticResource ClassicNetworkTemplate}" />
                <Style.Triggers>
                    <DataTrigger Binding="{Binding Path=(themes:ThemeContext.CurrentTheme), RelativeSource={RelativeSource Self}}"
                                 Value="{x:Static models:AppTheme.Tracework}">
                        <Setter Property="ContentTemplate" Value="{StaticResource TraceworkNetworkTemplate}" />
                    </DataTrigger>
                </Style.Triggers>
            </Style>
        </ContentControl.Style>
    </ContentControl>
</UserControl>
