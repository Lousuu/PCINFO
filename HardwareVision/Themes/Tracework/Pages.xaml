<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:HardwareVision.Controls">
    <Style x:Key="TraceworkPageRootStyle" TargetType="FrameworkElement">
        <Setter Property="Margin" Value="0" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
    </Style>

    <Style x:Key="TraceworkPageScrollViewerStyle" TargetType="ScrollViewer">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="PanningMode" Value="VerticalOnly" />
        <Setter Property="Padding" Value="0,0,12,24" />
    </Style>

    <Style x:Key="TraceworkPageContentHostStyle" TargetType="Border">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Stretch" />
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="0" />
        <Setter Property="Padding" Value="0,0,12,24" />
    </Style>

    <Style x:Key="TraceworkPageContentStackStyle"
           TargetType="StackPanel"
           BasedOn="{StaticResource RetroPageStackStyle}">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>

    <Style x:Key="TraceworkEditorialPageHeaderStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,0,24" />
        <Setter Property="Padding" Value="0,0,0,12" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>

    <Style x:Key="TraceworkDisplayValueTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="48" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkTelemetryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
    </Style>

    <Style x:Key="TraceworkPageIndexTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="36" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkIdentityBrush}" />
    </Style>

    <Style x:Key="TraceworkSectionTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="16" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkPaperBrush}" />
    </Style>

    <Style x:Key="TraceworkBodyValueTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="12" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkPaperBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkAnnotationTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkDustGreyBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkInstrumentFieldStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="236" />
        <Setter Property="Padding" Value="20,18,16,16" />
        <Setter Property="Background" Value="{DynamicResource TraceworkTechnicalGridBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkIdentityBrush}" />
        <Setter Property="BorderThickness" Value="2,0,0,1" />
    </Style>

    <Style x:Key="TraceworkInstrumentLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkIdentityBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkInstrumentValueStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource TraceworkDisplayValueTextStyle}" />

    <Style x:Key="TraceworkInstrumentUnitStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Bottom" />
        <Setter Property="Margin" Value="8,0,0,8" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkTextSecondaryBrush}" />
    </Style>

    <Style x:Key="TraceworkDataRailStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,24,0,0" />
        <Setter Property="Padding" Value="0" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="0,1,0,1" />
    </Style>

    <Style x:Key="TraceworkDataRailItemStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="150" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
    </Style>

    <Style x:Key="TraceworkDataRailLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkIdentityBrush}" />
    </Style>

    <Style x:Key="TraceworkDataRailValueStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource TraceworkBodyValueTextStyle}">
        <Setter Property="Margin" Value="0,4,0,0" />
    </Style>

    <Style x:Key="TraceworkAnnotationRailStyle" TargetType="Border">
        <Setter Property="Padding" Value="12,4,0,4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkTraceGreyBrush}" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <Style x:Key="TraceworkChartFieldStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="300" />
        <Setter Property="Padding" Value="18,16" />
        <Setter Property="Background" Value="{DynamicResource TraceworkSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkTelemetryBrush}" />
        <Setter Property="BorderThickness" Value="2,0,0,1" />
    </Style>

    <Style x:Key="TraceworkPrimaryChartStyle" TargetType="controls:RealtimeLineChart">
        <Setter Property="MinHeight" Value="210" />
        <Setter Property="LineBrush" Value="{DynamicResource TraceworkTelemetryBrush}" />
        <Setter Property="GridLineBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="TextBrush" Value="{DynamicResource TraceworkDustGreyBrush}" />
    </Style>

    <Style x:Key="TraceworkMicroChartStyle" TargetType="controls:RealtimeLineChart">
        <Setter Property="MinHeight" Value="76" />
        <Setter Property="LineBrush" Value="{DynamicResource TraceworkTelemetryBrush}" />
        <Setter Property="GridLineBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="TextBrush" Value="{DynamicResource TraceworkDustGreyBrush}" />
    </Style>

    <Style x:Key="TraceworkChartStatisticRailStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,8,0,0" />
        <Setter Property="Padding" Value="0,8,0,0" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="0,1,0,0" />
    </Style>

    <Style x:Key="TraceworkMicroChannelStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="128" />
        <Setter Property="Padding" Value="12,10" />
        <Setter Property="Background" Value="{DynamicResource TraceworkSurfaceSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
    </Style>

    <Style x:Key="TraceworkSignalMatrixStyle"
           TargetType="DataGrid"
           BasedOn="{StaticResource RetroSensorDataGridStyle}">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="Transparent" />
    </Style>

    <Style x:Key="TraceworkPageHeaderStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,0,10" />
        <Setter Property="Padding" Value="14,10" />
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="2" />
    </Style>

    <Style x:Key="TraceworkPageCodeTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style x:Key="TraceworkPageTitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="20" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    </Style>

    <Style x:Key="TraceworkPageSubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
    </Style>

    <Style x:Key="TraceworkPanelStyle" TargetType="controls:TraceworkPanel">
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="12" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Stretch" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="controls:TraceworkPanel">
                    <Grid>
                        <Border x:Name="PanelBorder"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="2">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="*" />
                                </Grid.RowDefinitions>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{TemplateBinding Code}"
                                               Style="{DynamicResource TraceworkPanelCodeTextStyle}" />
                                    <TextBlock Grid.Column="1"
                                               Margin="10,0,8,0"
                                               Text="{TemplateBinding Title}"
                                               Style="{DynamicResource TraceworkPanelTitleTextStyle}" />
                                    <Border Grid.Column="2"
                                            MinWidth="{TemplateBinding BadgeMinWidth}"
                                            HorizontalAlignment="{TemplateBinding BadgeHorizontalAlignment}"
                                            VerticalAlignment="{TemplateBinding BadgeVerticalAlignment}">
                                        <TextBlock HorizontalAlignment="Right"
                                                   Text="{TemplateBinding BadgeText}"
                                                   Style="{DynamicResource TraceworkPortLabelStyle}" />
                                    </Border>
                                </Grid>

                                <Grid Grid.Row="1" Margin="0,4,0,9">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Text="{TemplateBinding Subtitle}"
                                               ToolTip="{TemplateBinding Subtitle}"
                                               Style="{DynamicResource TraceworkPanelSubtitleTextStyle}" />
                                    <TextBlock Grid.Column="1"
                                               Margin="10,0,0,0"
                                               Text="{TemplateBinding HeaderNote}"
                                               ToolTip="{TemplateBinding HeaderNote}"
                                               Style="{DynamicResource TraceworkPanelSourceTextStyle}" />
                                </Grid>

                                <ContentPresenter Grid.Row="2"
                                                  Content="{TemplateBinding Content}"
                                                  ContentTemplate="{TemplateBinding ContentTemplate}"
                                                  ContentTemplateSelector="{TemplateBinding ContentTemplateSelector}"
                                                  HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                                  VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                                  SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                            </Grid>
                        </Border>

                        <Path Width="12"
                              Height="12"
                              HorizontalAlignment="Right"
                              VerticalAlignment="Bottom"
                              Data="M0,12 L12,0 L12,12 Z"
                              Fill="{DynamicResource AppBackgroundBrush}"
                              Stroke="{TemplateBinding BorderBrush}"
                              StrokeThickness="1" />
                        <Border Width="24"
                                Height="2"
                                Margin="10,0,0,0"
                                HorizontalAlignment="Left"
                                VerticalAlignment="Top"
                                Background="{DynamicResource AccentBrush}" />
                    </Grid>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="PanelVariant" Value="Primary">
                <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
            </Trigger>
            <Trigger Property="PanelVariant" Value="Muted">
                <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TraceworkHeroPanelStyle"
           TargetType="controls:TraceworkPanel"
           BasedOn="{StaticResource TraceworkPanelStyle}">
        <Setter Property="PanelVariant" Value="Primary" />
        <Setter Property="MinHeight" Value="208" />
    </Style>

    <Style x:Key="TraceworkSecondaryPanelStyle"
           TargetType="controls:TraceworkPanel"
           BasedOn="{StaticResource TraceworkPanelStyle}">
        <Setter Property="PanelVariant" Value="Default" />
        <Setter Property="MinHeight" Value="164" />
    </Style>

    <Style x:Key="TraceworkPanelCodeTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style x:Key="TraceworkPanelTitleTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkPanelSubtitleTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkPanelSourceTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="MaxWidth" Value="180" />
    </Style>

    <Style x:Key="TraceworkHeroMetricLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkHeroMetricValueStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="27" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkMetricRowLabelStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkMetricRowValueStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkDataStripStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="10,7" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkDataStripLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style x:Key="TraceworkDataStripValueStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="8,0,0,0" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkStatusNotchStyle" TargetType="Border">
        <Setter Property="Padding" Value="8,3" />
        <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkPortLabelStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
    </Style>

    <Style x:Key="TraceworkDividerStyle" TargetType="Border">
        <Setter Property="Height" Value="1" />
        <Setter Property="Margin" Value="0,6" />
        <Setter Property="Background" Value="{DynamicResource DividerBrush}" />
    </Style>

    <Style x:Key="TraceworkDashboardSelectorStyle"
           TargetType="ComboBox"
           BasedOn="{StaticResource DashboardHardwareComboBox}">
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="MinHeight" Value="30" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>

    <Style x:Key="TraceworkChartGridStyle" TargetType="ItemsControl">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>

    <Style x:Key="TraceworkChartCardStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="178" />
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="10" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkChartHeaderTextStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkChartCurrentValueStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="13" />
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkChartStatisticStyle" TargetType="TextBlock">
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkChartWindowSelectorStyle"
           TargetType="ComboBox"
           BasedOn="{StaticResource DashboardHardwareComboBox}">
        <Setter Property="Width" Value="104" />
        <Setter Property="MinWidth" Value="104" />
        <Setter Property="MaxWidth" Value="104" />
        <Setter Property="HorizontalAlignment" Value="Right" />
    </Style>

    <Style x:Key="TraceworkDataTablePanelStyle"
           TargetType="controls:TraceworkPanel"
           BasedOn="{StaticResource TraceworkPanelStyle}">
        <Setter Property="MinHeight" Value="210" />
        <Setter Property="PanelVariant" Value="Muted" />
    </Style>

    <Style x:Key="TraceworkTechnicalPanelStyle"
           TargetType="controls:TraceworkPanel"
           BasedOn="{StaticResource TraceworkPanelStyle}" />

    <Style x:Key="TraceworkDataGridStyle"
           TargetType="DataGrid"
           BasedOn="{StaticResource RetroSensorDataGridStyle}">
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="HorizontalGridLinesBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="VerticalGridLinesBrush" Value="{DynamicResource DividerBrush}" />
    </Style>

    <Style x:Key="TraceworkDataGridHeaderStyle"
           TargetType="DataGridColumnHeader"
           BasedOn="{StaticResource SensorTableHeaderCenterStyle}">
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
    </Style>

    <Style x:Key="TraceworkRowsBadgeStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="62" />
        <Setter Property="Padding" Value="8,3" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkRowsBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style x:Key="TraceworkSensorNameTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SensorTableNameTextStyle}">
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    </Style>

    <Style x:Key="TraceworkSensorTypeTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SensorTableTypeTextStyle}">
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
    </Style>

    <Style x:Key="TraceworkSensorValueTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SensorTableReadoutTextStyle}">
        <Setter Property="FontWeight" Value="SemiBold" />
        <Setter Property="Foreground" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style x:Key="TraceworkSensorStatusTextStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource SensorTableStatusTextStyle}">
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
    </Style>

    <Style x:Key="TraceworkGpuSelectorPanelStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="12,9" />
        <Setter Property="Background" Value="{DynamicResource PanelBackgroundBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkGpuSelectorStyle"
           TargetType="ComboBox"
           BasedOn="{StaticResource FutureComboBox}">
        <Setter Property="MinWidth" Value="280" />
        <Setter Property="MaxWidth" Value="520" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>

    <Style x:Key="TraceworkDeviceSelectorStyle"
           TargetType="ComboBox"
           BasedOn="{StaticResource TraceworkGpuSelectorStyle}">
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MaxWidth" Value="640" />
    </Style>

    <Style x:Key="TraceworkIdentityCardStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="58" />
        <Setter Property="Margin" Value="0,0,8,8" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkIdentityLabelStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontWeight" Value="Bold" />
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
    </Style>

    <Style x:Key="TraceworkIdentityValueStyle" TargetType="TextBlock">
        <Setter Property="Margin" Value="0,5,0,0" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
    </Style>

    <Style x:Key="TraceworkMemoryMetricCardStyle" TargetType="ContentControl">
        <Setter Property="MinWidth" Value="0" />
        <Setter Property="MinHeight" Value="92" />
        <Setter Property="Padding" Value="16,12" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ContentControl">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="1">
                        <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TraceworkNodeListStyle" TargetType="ItemsControl">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>

    <Style x:Key="TraceworkNodePanelStyle" TargetType="Border">
        <Setter Property="Margin" Value="0,0,0,8" />
        <Setter Property="Padding" Value="11,9" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
        <Setter Property="HorizontalAlignment" Value="Stretch" />
    </Style>

    <Style x:Key="TraceworkModulePanelStyle"
           TargetType="Border"
           BasedOn="{StaticResource TraceworkNodePanelStyle}">
        <Setter Property="BorderBrush" Value="{DynamicResource AccentSoftBrush}" />
    </Style>

    <Style x:Key="TraceworkEmptyStateStyle" TargetType="Border">
        <Setter Property="MinHeight" Value="72" />
        <Setter Property="Padding" Value="14" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkEmptyStateTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="TextAlignment" Value="Center" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="11" />
        <Setter Property="Foreground" Value="{DynamicResource TextMutedBrush}" />
    </Style>

    <Style x:Key="TraceworkToggleStyle"
           TargetType="CheckBox"
           BasedOn="{StaticResource ToggleSwitch}">
        <Setter Property="Margin" Value="0,8,0,0" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
    </Style>

    <Style x:Key="TraceworkDeviceStatusBadgeStyle"
           TargetType="Border"
           BasedOn="{StaticResource StatusBadge}">
        <Setter Property="Margin" Value="8,0,0,0" />
        <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
    </Style>

    <Style x:Key="TraceworkNetworkBadgeStyle" TargetType="Border">
        <Setter Property="MinWidth" Value="74" />
        <Setter Property="Height" Value="28" />
        <Setter Property="Padding" Value="10,0" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
        <Setter Property="HorizontalAlignment" Value="Right" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="Background" Value="{DynamicResource TraceworkIdentitySoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkIdentityBrush}" />
        <Setter Property="TextElement.Foreground" Value="{DynamicResource TraceworkIdentityBrush}" />
    </Style>

    <Style x:Key="TraceworkNetworkStatusBadgeStyle"
           TargetType="Border"
           BasedOn="{StaticResource TraceworkNetworkBadgeStyle}">
        <Setter Property="Margin" Value="8,0,0,0" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding Device.IsUp}" Value="True">
                <Setter Property="Background" Value="{DynamicResource TraceworkActiveSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TraceworkActiveBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource TraceworkActiveBrush}" />
            </DataTrigger>
            <DataTrigger Binding="{Binding Device.IsUp}" Value="False">
                <Setter Property="Background" Value="{DynamicResource TraceworkFaultSoftBrush}" />
                <Setter Property="BorderBrush" Value="{DynamicResource TraceworkFaultBrush}" />
                <Setter Property="TextElement.Foreground" Value="{DynamicResource TraceworkFaultBrush}" />
            </DataTrigger>
        </Style.Triggers>
    </Style>

    <Style x:Key="TraceworkNetworkBadgeTextStyle" TargetType="TextBlock">
        <Setter Property="HorizontalAlignment" Value="Center" />
        <Setter Property="VerticalAlignment" Value="Center" />
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="9" />
        <Setter Property="FontWeight" Value="Bold" />
    </Style>

    <Style x:Key="TraceworkDenseMetricGridStyle" TargetType="ItemsControl">
        <Setter Property="HorizontalAlignment" Value="Stretch" />
        <Setter Property="VerticalAlignment" Value="Top" />
    </Style>

    <Style x:Key="TraceworkAddressTextStyle" TargetType="TextBlock">
        <Setter Property="FontFamily" Value="{DynamicResource AppFontFamily}" />
        <Setter Property="FontSize" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
        <Setter Property="TextTrimming" Value="CharacterEllipsis" />
        <Setter Property="TextWrapping" Value="NoWrap" />
    </Style>

    <Style x:Key="TraceworkConfigurationPanelStyle"
           TargetType="controls:TraceworkPanel"
           BasedOn="{StaticResource TraceworkPanelStyle}">
        <Setter Property="MinHeight" Value="148" />
        <Setter Property="PanelVariant" Value="Muted" />
    </Style>

    <Style x:Key="TraceworkConfigurationActionStyle"
           TargetType="Button"
           BasedOn="{StaticResource FutureButton}">
        <Setter Property="MinWidth" Value="112" />
        <Setter Property="Margin" Value="8,0,0,0" />
    </Style>

    <Style x:Key="TraceworkConfigurationInputStyle"
           TargetType="TextBox"
           BasedOn="{StaticResource SettingsInputBox}">
        <Setter Property="Width" Value="72" />
    </Style>

    <Style x:Key="TraceworkConfigurationStepperStyle"
           TargetType="Button"
           BasedOn="{StaticResource SettingsStepperButton}" />

    <Style x:Key="TraceworkThemeCardStyle"
           TargetType="ToggleButton"
           BasedOn="{StaticResource ThemeCardToggleButtonStyle}">
        <Setter Property="MinHeight" Value="112" />
    </Style>

    <Style x:Key="TraceworkPathValueStyle"
           TargetType="TextBlock"
           BasedOn="{StaticResource TraceworkAddressTextStyle}">
        <Setter Property="Margin" Value="0,5,0,0" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
    </Style>

    <Style x:Key="TraceworkMetricCategoryListStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
    </Style>

    <Style x:Key="TraceworkMetricCategoryItemStyle" TargetType="ListBoxItem">
        <Setter Property="Margin" Value="0,0,0,7" />
        <Setter Property="Padding" Value="10,8" />
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}" />
        <Setter Property="Background" Value="{DynamicResource CardBackgroundSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource DividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <Border Padding="{TemplateBinding Padding}"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="1">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="{DynamicResource AccentSoftBrush}" />
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}" />
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter Property="BorderBrush" Value="{DynamicResource AccentBrush}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="TraceworkMetricCatalogRowStyle"
           TargetType="Border"
           BasedOn="{StaticResource TraceworkNodePanelStyle}">
        <Setter Property="Padding" Value="10,7" />
    </Style>

    <Style x:Key="TraceworkMetricCatalogListStyle" TargetType="ListBox">
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderThickness" Value="0" />
        <Setter Property="ScrollViewer.CanContentScroll" Value="True" />
        <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
        <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
        <Setter Property="VirtualizingPanel.IsVirtualizing" Value="True" />
        <Setter Property="VirtualizingPanel.VirtualizationMode" Value="Recycling" />
    </Style>

    <Style x:Key="TraceworkMetricCatalogItemStyle" TargetType="ListBoxItem">
        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsTabStop" Value="False" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ListBoxItem">
                    <ContentPresenter />
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Full-page expansion primitives. These remain static, data-driven surfaces. -->
    <Style x:Key="TraceworkCapacityFieldStyle" TargetType="Border">
        <Setter Property="Padding" Value="22" />
        <Setter Property="Background" Value="{DynamicResource TraceworkSurfaceSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="1,0,0,1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkCapacityTrackStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="8" />
        <Setter Property="Minimum" Value="0" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Foreground" Value="{DynamicResource TraceworkTelemetryBrush}" />
        <Setter Property="Background" Value="{DynamicResource TraceworkSurfaceRaisedBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="Focusable" Value="False" />
        <Setter Property="IsHitTestVisible" Value="False" />
    </Style>

    <SolidColorBrush x:Key="TraceworkCapacityUsedBrush" Color="{DynamicResource TraceworkSignalCyanColor}" />
    <SolidColorBrush x:Key="TraceworkCapacityFreeBrush" Color="{DynamicResource TraceworkSurfaceRaisedColor}" />

    <Style x:Key="TraceworkTopologyMatrixStyle"
           TargetType="ItemsControl"
           BasedOn="{StaticResource TraceworkNodeListStyle}">
        <Setter Property="Focusable" Value="False" />
    </Style>

    <Style x:Key="TraceworkTopologyNodeStyle"
           TargetType="Border"
           BasedOn="{StaticResource TraceworkNodePanelStyle}">
        <Setter Property="Padding" Value="14,11" />
        <Setter Property="BorderThickness" Value="0,0,0,1" />
        <Setter Property="Background" Value="Transparent" />
    </Style>

    <Style x:Key="TraceworkControlWorkspaceStyle" TargetType="Border">
        <Setter Property="Padding" Value="18" />
        <Setter Property="Background" Value="{DynamicResource TraceworkSurfaceBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkDividerBrush}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkControlRailStyle" TargetType="Border">
        <Setter Property="Padding" Value="0,4,16,4" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkIdentityBrush}" />
        <Setter Property="BorderThickness" Value="0,0,1,0" />
    </Style>

    <Style x:Key="TraceworkTimelineFieldStyle" TargetType="Border">
        <Setter Property="Padding" Value="18" />
        <Setter Property="Background" Value="{DynamicResource TraceworkSurfaceSoftBrush}" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkTelemetryBrush}" />
        <Setter Property="BorderThickness" Value="1,0,0,0" />
        <Setter Property="CornerRadius" Value="1" />
    </Style>

    <Style x:Key="TraceworkIdentityPlateStyle" TargetType="Border">
        <Setter Property="Padding" Value="18" />
        <Setter Property="Background" Value="Transparent" />
        <Setter Property="BorderBrush" Value="{DynamicResource TraceworkIdentityBrush}" />
        <Setter Property="BorderThickness" Value="2,0,0,1" />
        <Setter Property="CornerRadius" Value="0" />
    </Style>
</ResourceDictionary>
