# 《TRACEWORK 鹰角式视觉语言研究与重构规范》

**版本：** 1.0
**适用项目：** HardwareVision / PCINFO
**适用范围：** Tracework 主题的静态版式、组件视觉、数据表达、页面构图与响应式规则
**不包含：** Classic 主题重构、FLOW RELAY 重写、启动动画、采集业务调整

---

## 0. 规范定位

本规范不是鹰角网络的官方设计手册，也不试图复制《明日方舟》或《明日方舟：终末地》的专有界面。

它建立在三类材料之上：

1. **鹰角官方公开表达与官方页面**：用于确认品牌价值观、世界构建意识、信息系统化倾向及 UI 在游戏设计中的角色。
2. **公开作品中可重复观察的视觉规律**：用于归纳版式、层级、编号、信息密度与页面差异化方法。
3. **ignoredone’s space 等社区研究资料**：用于补充动线、版式、留白和案例分析方法。

鹰角官方使用“充满希望的逻辑艺术事业”描述自身创作方向，并强调构建“完整且独一无二的世界”；官方《明日方舟》网站也把内容组织为 INDEX、INFORMATION、OPERATOR、WORLD、MEDIA 等明确的信息章节。由此可见，值得借鉴的核心不是若干斜线、英文或边框，而是**逻辑系统、艺术表达和世界内信息组织的统一**。([鹰角网络招聘官网][1])

鹰角公开的 UI 创作幕后内容以“在游戏设计里穿针引线”为题，并明确讨论游戏内外不同 UI 的创作过程。这表明 UI 的作用更接近连接玩法、叙事、美术和交互的中间系统，而非覆盖在功能之上的装饰皮肤。([Sina Mobile][2])

---

# 第一部分：研究结论

## 1. 鹰角式视觉的核心并非“科技感”

“科技感”只是部分作品的表层结果。更稳定的母体是：

```text
内容语义
→ 信息结构
→ 世界内身份
→ 版式关系
→ 视觉装饰
```

错误顺序则是：

```text
斜线
+ 英文
+ 编号
+ 黑白灰
+ 亮色点缀
= 鹰角风
```

后者只能形成模仿痕迹，无法形成可信的系统。

### 对 PCINFO 的定义

TRACEWORK 应被理解为：

> 一套由“迹构系统”运行的硬件观测、信号诊断和会话记录终端。

各页面不是相同仪表盘的不同数据源，而是同一系统下的不同工作站：

* Dashboard：系统拓扑总览站
* CPU：处理器信号诊断站
* GPU：图形管线与渲染遥测站
* Memory：模组与槽位拓扑站
* Disk：存储介质与健康档案站
* Network：接口与链路路由站
* Motherboard：平台身份与固件登记站
* Game：目标进程与性能捕获站
* Report：会话事件与诊断报告站
* Settings：系统控制与策略配置站

---

## 2. “统一品牌”不等于“所有页面同一种模板”

鹰角早期官方招聘专题公开使用“每一个系列都是新的开始”，同时强调持续构建独一无二的世界。这支持一种设计思路：底层品牌规则保持一致，但不同主题、内容和系统拥有各自的局部视觉语法。([Arknights][3])

《终末地》主创公开访谈也提到，复杂系统在不同平台上不能简单共用同一套界面，一些界面需要针对键鼠、手柄和移动端分别定制交互布局。这说明高一致性不应建立在机械复用之上，而应建立在共同原则下的场景适配。([Weibo][4])

### 对 TRACEWORK 的结论

需要统一的是：

* 字体系统
* 色彩语义
* 编号体系
* 线宽规则
* 状态表达
* 交互反馈
* 间距节奏
* 可访问性

不需要统一的是：

* 每页模块比例
* 每页数据密度
* 每页首屏构图
* 每页图表组织方式
* 每页容器数量
* 每页视觉重心

---

## 3. 版式先于装饰

ignoredone’s space 对“页面显得空”的回答强调：新增元素应服从视觉动线；主体在哪里，元素就围绕主体展开；留白可能本身就是版式的一部分，浅纹理只能提升质感，无法替代版式。([Ignored One][5])

### 对当前 TRACEWORK 的诊断

当前页面的问题不是元素太少，而是：

* 主体不够明确；
* 元素权重过于平均；
* 大部分模块使用相同完整边框；
* 等宽布局比例过高；
* 密度变化不足；
* 留白没有成为结构；
* 页面之间缺乏专属构图。

所以不得通过继续添加随机小字、扫描线、纹理和装饰编号解决。

---

## 4. 视觉应具有“编辑设计”而非“控件库展示”特征

官方《明日方舟》网站使用清晰的章节英文、中文内容、索引和信息分类建立编辑式层级。它看起来更像一份持续更新的世界档案，而不只是普通导航栏加内容卡片。([Arknights][6])

### 编辑式界面的典型特征

* 超大字号和极小字号并存；
* 文本、编号和图形共享构图职责；
* 局部对齐严格，但全页不追求镜像对称；
* 主要内容跨越多个栅格；
* 辅助信息压到边缘或轴线上；
* 页面存在清晰阅读顺序；
* 不同区域密度明显不同。

---

## 5. 功能与美术必须互相解释

鹰角官方强调“逻辑艺术”以及完整世界构建；因此真正可借鉴的方法是让视觉元素具备系统理由。([鹰角网络招聘官网][1])

例如：

| 视觉元素 | 合理含义           |
| ---- | -------------- |
| 大编号  | 页面、设备、槽位或通道身份  |
| 连接线  | 数据来源、设备关系或读取路径 |
| 切角   | 端口、接口、插槽或状态锁位  |
| 状态色  | 正常、活动、警告、异常    |
| 微型注释 | 单位、采样窗口、来源和时间  |
| 纵向刻度 | 通道序号、时间刻度或容量区间 |
| 空白区域 | 分离不同认知任务       |

不得添加无法解释的：

* 假坐标；
* 假序列号；
* 假命令；
* 无意义英文段落；
* 随机警告符号；
* 与真实状态无关的红色元素。

---

# 第二部分：重构总目标

## 6. 最终设计目标

TRACEWORK 静态视觉应实现：

```text
可信的系统身份
+
严格但不均匀的栅格
+
清晰的主次信息
+
页面专属构图
+
结构化留白
+
真实数据驱动的标注
+
低成本、可维护的 WPF 实现
```

### 用户第一眼应获得

1. 当前处于哪个系统页面；
2. 本页最重要的状态是什么；
3. 哪些指标正在变化；
4. 哪些数据属于身份信息；
5. 哪些区域可以交互；
6. 是否存在异常或限制条件。

### 用户不应感到

* 所有卡片都同等重要；
* 页面只是很多矩形向下堆叠；
* 每页仅换了标题；
* 英文和编号只是装饰；
* 信息很满但没有阅读顺序。

---

## 7. 设计原则

### P1. 单一视觉主语

每个首屏只能有一个主要视觉主语。

例如：

* CPU：Package 当前主指标
* GPU：核心温度或功耗与渲染状态
* Memory：总容量及槽位占用
* Disk：当前设备健康与容量
* Network：活动链路与实时吞吐
* Game：目标进程和捕获状态

其他信息均为解释、支撑或细化。

---

### P2. 受控不对称

使用严格底层栅格，但禁止无理由的完全平均分配。

建议常用比例：

```text
8 : 4
7 : 5
5 : 3 : 4
8 : 2 : 2
```

谨慎使用：

```text
6 : 6
3 : 3 : 3 : 3
```

只有比较任务确实要求等权时，才使用等宽结构。

---

### P3. 密度分区

每页至少包含三种密度区域：

* **高密度区**：指标列表、通道矩阵、细节参数；
* **中密度区**：主要图表、状态摘要；
* **低密度区**：主数值、页面身份、结构化留白。

禁止全页维持相同信息密度。

---

### P4. 层级跨度

至少建立五级文字层级，而不是让大部分文本集中在 9–13px。

建议：

| 等级         |      尺寸 | 用途          |
| ---------- | ------: | ----------- |
| Display    | 40–52px | 页面核心实时数值    |
| Index      | 28–40px | 页面或设备大编号    |
| Module     | 14–17px | 模块标题        |
| Body       | 11–13px | 主要参数与控制     |
| Annotation |   8–9px | 单位、来源、通道、说明 |

大字号不得泛滥。每个首屏通常只允许一个 Display 层级。

---

### P5. 容器节制

完整矩形边框的面积覆盖率目标：

```text
首屏区域：30%–50%
```

其余信息采用开放式结构：

* 单侧边线；
* L 形角标；
* 顶部导轨；
* 底部标尺；
* 无背景数据簇；
* 共享边框的组合区域；
* 轴线分区。

禁止每一个模块都拥有独立背景、完整边框和相同标题栏。

---

### P6. 真实编号

编号只能来自：

* 页面代码；
* 设备稳定 ID；
* 通道索引；
* 插槽序号；
* 接口序号；
* 可验证的硬件标识；
* 稳定的 UI 模块代码。

允许：

```text
CPU.01
PKG / 00
CORE / 07
MEM / SLOT 01
NET / IF 02
GPU / DEV 10DE-xxxx
SAMPLE / 60 SEC
```

不允许：

```text
X-0928
SECTOR-ALPHA
COMMAND 77
UNKNOWN PROTOCOL
```

除非它们有真实业务来源。

---

### P7. 视觉关系来自数据关系

如果两个数据没有业务关系，不使用连接线连接。

如果数据存在明确关系，应优先用位置表达：

```text
设备
└─ 模组
   └─ 指标
```

或：

```text
总容量
├─ 已用
├─ 可用
└─ 缓存
```

关系优先通过层级、距离、对齐和共享轴表达，最后才使用连线。

---

### P8. 页面有独立语法

每类页面必须拥有自己的构图逻辑，不得全都套用：

```text
Page Header
Data Strip
Hero Panel
Chart Panel
Table Panel
```

---

# 第三部分：视觉基础系统

## 8. 栅格系统

### 8.1 桌面基准

PageHost 内容区采用 12 列逻辑栅格。

建议：

```text
列间距：12px
基础纵向节奏：8px
模块主间距：16px
大分区间距：24px
页面右/底安全区：保持现有规则
```

### 8.2 栅格使用

* 主视觉：7–9 列；
* 辅助状态：3–5 列；
* 微型参数：2–3 列；
* 页面边缘标注：1 列或绝对窄轨；
* 重要内容允许跨行。

### 8.3 禁止

* 无目的任意偏移；
* 为追求不对称而破坏对齐；
* 同一页面出现多个不兼容基准线；
* 小窗口仍强制保持 12 列视觉。

---

## 9. 响应式断点

建议采用内容导向断点：

### Wide

```text
可用宽度 >= 1360px
12 列
完整非对称构图
边缘注释轨保留
```

### Standard

```text
960–1359px
8 列或压缩 12 列
保留主次比例
减少边缘注释
```

### Compact

```text
680–959px
4 列
主区域与辅助区域上下重排
禁止小块并排过多
```

### Narrow

```text
<680px
单列主流程
保留层级差
取消不必要装饰轨
```

响应式时可以改变布局，但不得改变指标含义、命令、选中状态和页面生命周期。

---

## 10. 色彩系统

保留现有 Tracework 基础色，但重新限制使用比例。

### 背景

* MachineBlack：全局背景与最高层暗面；
* PanelBackground：重要结构区；
* CardBackgroundSoft：次级数据簇；
* 不再为所有模块铺背景。

### IonViolet / Accent

用于：

* 当前选中；
* 主通道；
* 页面核心索引；
* 数据流起点；
* 关键交互。

同一首屏高饱和 Accent 面积应尽量低于约 8%。

### PhosphorMint

仅用于：

* 成功锁定；
* 活动状态；
* 正常且实时；
* 已提交或已连接。

不得作为普通装饰色大面积使用。

### 警告色

只与真实阈值或错误状态绑定。

禁止为了“工业感”常驻红色警示。

---

## 11. 线条系统

建议固定三档：

| 类型         |        线宽 | 用途        |
| ---------- | --------: | --------- |
| Structural |       1px | 主栅格、模块分区  |
| Active     |       2px | 当前通道、选中边缘 |
| Lock       | 3px 或局部节点 | 短时锁定和高优先级 |

同一容器不得同时堆叠多重无意义描边。

---

## 12. 形状系统

允许：

* 单角切口；
* L 形边角；
* 水平导轨；
* 垂直索引轴；
* 局部斜切；
* 小型锁点；
* 端口状短线；
* 矩形和直线为主的几何。

禁止：

* 每个卡片都使用相同右下角切口；
* 大量不同角度的装饰斜线；
* 无意义六边形；
* 与硬件语义无关的复杂徽章；
* 模仿鹰角 Logo、罗德岛标志或阵营符号。

---

# 第四部分：组件重构

## 13. 将单一 TraceworkPanel 拆成视觉原语

当前 `TraceworkPanel` 不应继续承担所有内容。

建议建立以下原语。

### 13.1 InstrumentField

开放式核心数据显示区。

特征：

* 无完整背景或仅弱背景；
* 一个超大数值；
* 标签、单位和状态沿边缘排列；
* 可以带一条主轴或刻度；
* 用于 CPU/GPU/Network 核心状态。

---

### 13.2 TechnicalPanel

保留完整边框的标准技术模块。

仅用于：

* 需要明确边界的控制；
* 独立设备；
* 可滚动表格；
* 用户可交互模块；
* 报告摘要。

使用数量必须受限。

---

### 13.3 DataRail

取代大量四等分 DataStrip。

特征：

* 沿一条水平或垂直轴组织；
* 多个值共享背景或共享底线；
* 标签与数值不再每项一个独立框；
* 适合身份信息、采样窗口和状态。

---

### 13.4 AnnotationRail

窄型边缘标注轨。

内容：

* 页面代码；
* 数据更新时间；
* 采样频率；
* 设备索引；
* 数据来源；
* 当前模式。

不承载主要操作。

---

### 13.5 SignalMatrix

用于核心、通道、插槽、适配器和传感器矩阵。

特征：

* 共享列标题；
* 共享基线；
* 行级弱分隔；
* 不为每项创建卡片；
* 允许局部状态标记。

---

### 13.6 ChartField

连续图表区域。

特征：

* 一个主要图表；
* 若干附属微图；
* 共享时间轴；
* AVG/MIN/MAX 沿边缘排布；
* 避免每张曲线独立成完整卡片。

---

## 14. 容器使用规则

每个首屏最多建议：

* 1 个主 InstrumentField；
* 1–2 个 TechnicalPanel；
* 1 个 DataRail；
* 1 个 AnnotationRail；
* 1 个 ChartField 或 SignalMatrix。

不是硬性组件数量，但不得恢复“十几张相同卡片”。

---

# 第五部分：页面原型

## 15. Overview 原型

适用：

* Dashboard

视觉目标：

* 非对称系统拓扑；
* 一个核心健康/状态区域；
* CPU/GPU 为主要节点；
* Memory/Disk 为次级节点；
* 网络和更新时间为边缘状态；
* 不再使用 CPU/GPU 完全相等的镜像卡片。

建议结构：

```text
┌─────────────── PAGE ID / STATUS RAIL ───────────────┐
│                                                     │
│  SYSTEM STATE / 主状态        CPU 主节点             │
│  大值或状态                  跨越 5–6 列             │
│                             ┌──────────────────────┐ │
│                             │ GPU 辅助节点         │ │
│  MEMORY / DISK              ├──────────────────────┤ │
│  两个紧凑技术簇              │ LINK / EVENT         │ │
│                             └──────────────────────┘ │
└─────────────────────────────────────────────────────┘
```

---

## 16. Telemetry 原型

适用：

* CPU
* GPU

视觉目标：

* 主数值和连续曲线构成画面主体；
* 身份数据压缩为 DataRail；
* 详细通道形成 SignalMatrix；
* 图表不再全部是二等分卡片。

建议首屏：

```text
┌─ PAGE INDEX ────────────────────────────────────────┐
│  PACKAGE TELEMETRY        MAIN SIGNAL FIELD         │
│  大数值 / 单位            主曲线                    │
│  状态 / 当前窗口          主曲线                    │
│                           AVG / MIN / MAX           │
├───────────────┬─────────────────────────────────────┤
│ DEVICE ID     │ AUXILIARY CHANNELS                  │
│ TOPOLOGY      │ 3–4 个紧凑微型图                   │
└───────────────┴─────────────────────────────────────┘
```

详细核心矩阵在首屏之后。

---

## 17. Topology 原型

适用：

* Memory
* Disk
* Network
* Motherboard

### Memory

核心是：

```text
总容量
→ 已用/可用
→ 模组
→ 槽位
→ 频率与规格
```

模组应表现为槽位阵列，而不是若干普通卡片。

### Disk

核心是：

```text
物理设备
→ 分区
→ 容量
→ 温度
→ 健康与可靠性
```

### Network

核心是：

```text
活动适配器
→ 链路
→ 实时上下行
→ 地址和接口身份
```

连接线只用于真实适配器—链路关系。

### Motherboard

采用“设备铭牌 + 固件登记 + 机箱身份”结构，信息密度应低于 CPU/GPU 页。

---

## 18. Control 原型

适用：

* Settings
* Metric Visibility

建议结构：

```text
左侧：控制分类索引
右侧：当前设置组
底部或边缘：当前 Profile / Effective 状态
```

不再把每一组设置都纵向堆成同规格大面板。

必须保持：

* Tab 顺序；
* 键盘操作；
* 可访问名称；
* 控件命中区域；
* 设置保存语义。

---

## 19. Capture / Report 原型

适用：

* Game Performance
* Game Session Report

### Game Performance

视觉重心：

1. 目标进程；
2. 捕获状态；
3. 当前性能；
4. 限制条件；
5. 操作。

TARGET PROCESS 保持已经修复的输入对齐，但应作为一个操作站，而不是普通卡片。

### Session Report

采用：

* 结论摘要；
* 时间轴；
* 限制事件；
* 峰值和稳定性；
* 原始数据表。

时间轴应成为主要叙事轴。

---

# 第六部分：Dashboard 试点规范

## 20. Dashboard 重构目标

当前 CPU/GPU 两块完全对称，应改为明确主次。

### 推荐方案

#### 左侧 7 列

主系统场：

* 当前系统状态；
* CPU 主指标；
* 一条主要实时趋势；
* 大型页面索引或设备名称；
* 更新时间和采样状态。

#### 右侧 5 列

分为三段：

1. GPU 紧凑状态；
2. Memory/Disk 容量关系；
3. Network/事件状态。

#### 底部

开放式设备身份 DataRail：

```text
NODE
PLATFORM
UPDATED
STATE
```

四项共享一条轴，不再使用四个独立边框。

### 视觉层级

```text
第一：系统主状态或 CPU 当前核心值
第二：GPU 状态
第三：Memory/Disk
第四：身份和更新时间
```

---

# 第七部分：CPU 试点规范

## 21. CPU 重构目标

CPU 页面从“多个面板堆叠”改为“遥测工作台”。

### 首屏布局

#### 左上 4 列：Package Identity

* CPU 名称；
* 核心/线程；
* 当前采样窗口；
* 页面代码；
* 小型状态标记。

不使用完整大卡片，可用左侧主轴和底线。

#### 左中 4 列：Primary Metric

* 40–52px 主值；
* 单位独立；
* Label；
* Threshold/状态；
* 相关次级值。

#### 右侧 8 列：Primary Chart

* 一张主要曲线；
* 当前值右上；
* AVG/MIN/MAX 沿底部；
* 共享时间刻度；
* 不嵌套多个完整 ChartCard。

#### 首屏下部

3–4 个附属通道作为微图带，而不是二列完整卡片。

#### 次屏

CORE MATRIX：

* 保留数据表功能；
* 改为共享标题和弱行分隔；
* 大幅减少卡片感；
* 核心类型、名称、当前值保持可读。

---

# 第八部分：文字与标注

## 22. 中英文关系

英文负责：

* 分类；
* 系统身份；
* 模块代码；
* 技术短标签。

中文负责：

* 可理解的页面名称；
* 状态说明；
* 操作说明；
* 错误与提示。

禁止大量英语段落替代中文信息。

建议：

```text
CPU.01 / PROCESSOR SIGNAL
处理器信号
处理器封装与逐核心遥测
```

避免重复三遍同一语义。

---

## 23. 微型标注

允许的标注类型：

```text
SOURCE
WINDOW
UPDATED
DEVICE
CHANNEL
SLOT
ACTIVE
PAUSED
LIVE
HISTORY
```

每个标注必须有明确对象。

微型字不得低于可读底线。8–9px 只用于非关键内容，并必须具备足够对比度。

---

# 第九部分：图表规范

## 24. 图表层级

每页最多一张主要图表承担视觉主体。

附属图表：

* 使用 Sparkline；
* 减少重复坐标轴；
* 共享时间窗口；
* 统一采样语义；
* 仍显示当前值和单位。

### 禁止

* 每个指标一个相同大小图表卡片；
* 为装饰增加虚假曲线；
* 图表动画影响实时采样；
* 过强网格线；
* 颜色数量过多。

---

# 第十部分：动效与静态版式关系

## 25. FLOW RELAY 保持不变

当前 FLOW RELAY 已经完成。

静态重构不得：

* 重写导航状态机；
* 修改 Commit 时机；
* 改变 SignalRail 导航身份；
* 创建第二 PageHost；
* 破坏 Primary/Secondary 标记。

重构后的 Primary/Secondary 应重新选择真正的构图主体，而不是机械沿用旧容器。

---

## 26. 静态视觉优先

本阶段不继续增加：

* 新扫描动画；
* 循环噪点；
* 常驻闪烁；
* 卡片 Hover 位移；
* 随机数据流。

页面在完全静止时也必须成立。

---

# 第十一部分：可访问性与可用性

## 27. 强制要求

* 关键正文不低于 10–11px；
* 高优先级数据满足对比度要求；
* Accent 不作为唯一状态区分；
* 所有交互控件保留可见焦点；
* 装饰元素不进入 Tab 顺序；
* 装饰文本不重复进入 Automation 树；
* 小窗口不发生水平溢出；
* 长设备名称保留 Tooltip；
* 表格仍支持现有键盘和滚动行为；
* Reduced/Off 不影响静态结构。

“鹰角味”不得以降低可读性或增加操作成本为代价。复杂系统需要针对平台和输入方式定制布局，而不是只追求统一外观，这一点也与公开的《终末地》多平台交互实践一致。([Weibo][4])

---

# 第十二部分：性能与 WPF 实现边界

## 28. 允许

* Grid；
* Border；
* Path；
* Rectangle；
* Line；
* Geometry；
* SharedSizeGroup；
* 自定义轻量 Panel；
* 静态 DrawingBrush；
* 一次性简单动画；
* 现有 DynamicResource。

## 29. 禁止

* 大面积 Blur；
* PixelShader；
* 高频动态纹理；
* CompositionTarget.Rendering；
* DispatcherTimer 装饰动画；
* 大量嵌套透明层；
* 每个传感器行创建复杂 Path；
* 为版式复制 ViewModel；
* 页面截图或 VisualBrush 副本；
* 无意义的实时装饰更新。

---

# 第十三部分：实施顺序

## 30. Phase A：建立基础语言

只新增或重构共享资源：

* 12 列栅格辅助；
* InstrumentField；
* DataRail；
* AnnotationRail；
* SignalMatrix；
* ChartField；
* 开放式标题；
* 新字号等级；
* 容器变体；
* 线宽和状态规则。

不修改全部页面。

---

## 31. Phase B：两个试点页面

只重构：

1. Dashboard；
2. CPU。

原因：

* Dashboard 验证非对称 Overview；
* CPU 验证 Telemetry；
* 两者覆盖大部分共享组件；
* 避免一次性重做 12 个页面后难以调整方向。

---

## 32. Phase C：评审试点

人工检查：

* 1600×900；
* 2048×1189；
* 1366×768；
* 920×620；
* Windows 100% / 125% / 150% DPI。

评估：

* 首屏主语是否明确；
* 边框是否明显减少；
* 是否仍像卡片墙；
* 信息是否更容易扫描；
* 大小层级是否成立；
* 留白是否有结构；
* 动画是否仍与新布局匹配。

---

## 33. Phase D：扩展页面

通过试点后按顺序：

1. GPU；
2. Memory；
3. Network；
4. Disk；
5. Motherboard；
6. Game；
7. Report；
8. Settings；
9. Metric Visibility；
10. Advanced Sensors。

---

# 第十四部分：验收标准

## 34. 静态视觉验收

每个页面必须满足：

1. 首屏有且只有一个明确视觉主语；
2. 至少存在高、中、低三种密度区域；
3. 不再由同规格矩形连续堆叠构成；
4. 完整边框覆盖率显著下降；
5. 至少有一种页面专属构图；
6. 主要数据与辅助数据尺寸差异明显；
7. 所有编号有真实含义；
8. 所有连接线有真实关系；
9. 留白参与组织；
10. 没有为了风格添加的无意义英文。

---

## 35. 功能验收

必须保持：

* 所有原绑定；
* 所有命令；
* 所有设备选择；
* 所有指标显隐；
* 所有滚动；
* 所有图表更新；
* 所有页面生命周期；
* 所有设置持久化；
* FLOW RELAY；
* SYSTEM REWIRE；
* Classic；
* 单一 PageHost；
* 当前测试基线。

---

## 36. 失败判定

出现以下任一情况即视为重构失败：

* 只是更换边框形状；
* 只是加入更多斜线和英文；
* 页面仍是等宽卡片墙；
* 每页仍共享完全相同结构；
* 信息层级反而更弱；
* 主要实时值不易找到；
* 小窗口出现水平滚动；
* 装饰干扰图表；
* 假编号或假状态进入界面；
* 可访问性退化；
* 性能明显下降。

---

# 第十五部分：知识产权边界

## 37. 可以借鉴

* 逻辑驱动的视觉组织；
* 编辑式排版；
* 受控不对称；
* 大小层级差；
* 世界内信息系统；
* 编号与内容关系；
* 留白与密度对比；
* 页面主题差异化。

## 38. 不得复制

* 鹰角 Logo；
* 罗德岛、阵营或组织标志；
* 《明日方舟》专有图形；
* 官方活动标题字体造型；
* 特定海报的构图复刻；
* 角色、插画和纹样素材；
* 官方图标；
* 专有术语和世界观文本。

ignoredone’s space 可以作为学习案例和资料索引，但它属于社区设计与作品整理来源，不是官方品牌规范；其中的作品也不能直接作为项目素材使用。该站公开展示了大量《明日方舟》相关封面作品，并提供围绕动线、主体和留白的学习建议。([Ignored One][7])

---

# 最终设计定义

TRACEWORK 的目标不应是：

> 看起来像《明日方舟》的电脑监控软件。

最终目标应是：

> 一套拥有鹰角式逻辑艺术意识、编辑式信息层级和世界内系统感，同时保持自身品牌、真实硬件语义和桌面工具可用性的独立监控终端。

可以压缩为六条：

```text
逻辑决定装饰
内容决定版式
页面拥有主语
栅格允许不对称
留白参与结构
视觉服务真实数据
```

下一轮代码实施只应完成 **共享视觉原语 + Dashboard + CPU 两个试点**，暂不重构其余页面。这样才能在投入全量改造前，先确认新的静态视觉语言确实解决“死板、单调、卡片化”的问题。

[1]: https://career.hypergryph.com/?utm_source=chatgpt.com "鹰角网络招聘官网"
[2]: https://www.sina.cn/news/detail/5215896102241360.html?utm_source=chatgpt.com "朋友们大家好！在上期的《鹰角音乐创作幕后杂谈》发布后，我们看到很多朋友说希望听一听鹰角的UI设计师们分享他们的想法，讲讲鹰角游戏内外各式UI的创作幕后。因此本期#何以鹰角# ，我们邀请到了四位UI设计师 栊一水又、阿树、AZE 和 阿福，与大家分享一下鹰角的UI，是如何创作的。#鹰角网络# ​_新浪新闻"
[3]: https://ak.hypergryph.com/special/recruitment/?utm_source=chatgpt.com "JOIN US! - 鹰角网络"
[4]: https://www.weibo.com/ttarticle/p/show?id=2309405234813157376007&utm_source=chatgpt.com "不积跬步无以至千里：深度采访《明日方舟：终末地》三位主创"
[5]: https://www.ignoredone.space/index.php/answer/?utm_source=chatgpt.com "新手入门 – ignoredone's space"
[6]: https://ak.hypergryph.com/news?utm_source=chatgpt.com "情报中心 - 明日方舟"
[7]: https://www.ignoredone.space/index.php/portfolio/?utm_source=chatgpt.com "#作品库 – ignoredone's space"
